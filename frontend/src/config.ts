// ===========================================================================
//  config.ts -- Constantes de configuracion del frontend
// ===========================================================================
//
// Vive en su propio archivo (y no dentro de httpClient.ts) para que cripto.ts
// y httpClient.ts puedan usarla sin importarse en circulo el uno al otro.

/** Raiz de la API del backend. */
export const API_URL = 'http://localhost:8080/api'
